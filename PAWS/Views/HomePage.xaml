<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PAWS.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="40,32,40,24" Spacing="16" MaxWidth="840" HorizontalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Folders" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="Add account" Click="OnAddAccountClicked" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center" />
            </Grid>

            <StackPanel x:Name="AccountsPanel" Spacing="12" />

            <TextBlock
                FontSize="12"
                Opacity="0.6"
                TextWrapping="Wrap"
                Text="PAWS keeps syncing in the background when you close this window — reopen it any time from the tray icon." />
        </StackPanel>
    </ScrollViewer>
</Page>
